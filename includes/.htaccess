# Deny all direct web access to include files.
# These are meant to be require()'d by pages, never requested directly.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
